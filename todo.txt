problème du moyennage d'une année à l'autre : normalement c'est bon
pas d'attention entre les lags de sst car ils sont mélangés dès le départ (différent de ajouter à la suite des patchs ou concaténer embedding)
implémenter CNN pour baseline
run tout 
tester VAE pour le clustering plutot que pca (devrait donner la meme chose )
comparer juste SLP et SLP + SST pour voir s'il y a une plus value...

vit_with_slp_embedding load toutes les cartes de slp sur 2 mois sur tous les membres en RAM pour faire la PCA (=> beaucoup de RAM). ce n'est pas le cas de la pca membre par membre pour trouver les 4 clusters. 

réfléchir à la diffusion

relancer surtout avec embedding où ça n'a pas overfit. avec vae, avec moins de composantes. 